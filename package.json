{
  "scripts": {
    "clean": "rimraf node_modules && rimraf src/Api/ClientApp/node_modules && dotnet clean",
    "format": "dotnet csharpier . && prettier --write .",
    "format:check": "dotnet csharpier --check . && prettier --check ."
  },
  "devDependencies": {
    "@swc/cli": "^0.4.0",
    "@swc/core": "^1.7.3",
    "@vitejs/plugin-react": "^4.3.1",
    "prettier": "3.3.3",
    "rimraf": "^6.0.1"
  }
}
